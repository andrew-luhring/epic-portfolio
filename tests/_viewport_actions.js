define ([
	'require'
	, 'utility'
	, 'viewport_actions'
	, 'chai'
	, 'jquery'
	, 'jqueryui'
	, 'chaijq'], function (require, utility, viewport_actions, chai, jquery) {

	"use strict";

	var assert = chai.assert
		, expect = chai.expect
		, should = chai.should;


});
