define ([
	'require'
	, 'utility'
//	, 'image_resize'
	, 'chai'
	, 'jquery'
	, 'jqueryui'
	, 'chai_jq'], function (require, utility/*, image_resize*/, chai, jquery) {

	"use strict";

	var assert = chai.assert
			, expect = chai.expect
			, should = chai.should;


});
