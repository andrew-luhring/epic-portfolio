define ([
	'require'
	, 'utility'
//	, 'intro_animation'
	, 'chai'
	, 'jquery'
	, 'jqueryui'
	, 'chai_jq'], function (require, utility, /*intro_animation,*/ chai, jquery) {

	"use strict";

	var assert = chai.assert
		, expect = chai.expect
		, should = chai.should;


});
